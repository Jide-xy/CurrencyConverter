CREATE TABLE RateEntity  (
    currency_code TEXT NOT NULL PRIMARY KEY,
    base_currency_code TEXT NOT NULL,
    rate REAL NOT NULL DEFAULT 0.0
);

getAllRates:
SELECT *
FROM RateEntity;

insertRate:
INSERT OR REPLACE INTO RateEntity VALUES ?;